pre-commit:
  parallel: true
  commands:
    lint:
      glob: "*.{clj,edn}"
      run: DIRS="src dev test .clj-kondo/hooks" make lint-bin
    fmt:
      glob: "*.{clj,edn}"
      run: DIRS="src dev test .clj-kondo/hooks" make fmt-check-bin
